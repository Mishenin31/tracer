<UserControl x:Class="HabitTracker.Views.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="36,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Задачи" Style="{StaticResource H1}"/>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="14" Padding="20" Margin="0,20,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Что нужно сделать" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                    <TextBox x:Name="TitleBox" Style="{StaticResource InputBox}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Приоритет" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                    <ComboBox x:Name="PriorityCombo" Style="{StaticResource InputCombo}"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="Срок" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                    <DatePicker x:Name="DuePicker" Height="40" FontSize="14"
                                Foreground="Black" Background="White"/>
                </StackPanel>

                <Button Grid.Column="6" Content="Добавить" Style="{StaticResource AccentButton}"
                        VerticalAlignment="Bottom" Click="Add_Click"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="TasksGrid" Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="✓" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Click="Done_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="ЗАДАЧА" Binding="{Binding Title}" Width="*"
                                    ElementStyle="{StaticResource CellText}"/>
                <DataGridTextColumn Header="ПРИОРИТЕТ" Binding="{Binding PriorityDisplay}" Width="140"
                                    ElementStyle="{StaticResource CellText}"/>
                <DataGridTextColumn Header="СРОК" Binding="{Binding DueDisplay}" Width="130"
                                    ElementStyle="{StaticResource CellText}"/>
                <DataGridTemplateColumn Header="" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Удалить" Style="{StaticResource GhostButton}"
                                    Padding="12,6" FontSize="12" Height="32"
                                    VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Click="Delete_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
