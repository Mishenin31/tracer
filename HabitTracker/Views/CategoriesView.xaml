<UserControl x:Class="HabitTracker.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="36,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Категории" Style="{StaticResource H1}"/>
            <TextBlock Text="Общие категории привычек для всех пользователей" Style="{StaticResource Muted}"
                       FontSize="14" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="14" Padding="20" Margin="0,20,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Название" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                    <TextBox x:Name="NameBox" Style="{StaticResource InputBox}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Цвет (HEX)" Style="{StaticResource Muted}" Margin="0,0,0,6"/>
                    <TextBox x:Name="ColorBox" Style="{StaticResource InputBox}" Text="#7C6BAD"/>
                </StackPanel>

                <Button Grid.Column="4" Content="Добавить" Style="{StaticResource AccentButton}"
                        VerticalAlignment="Bottom" Click="Add_Click"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="Grid" Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="ЦВЕТ" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="22" Height="22" CornerRadius="6" Background="{Binding ColorHex}"
                                    HorizontalAlignment="Left"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="НАЗВАНИЕ" Binding="{Binding Name}" Width="*"
                                    ElementStyle="{StaticResource CellText}"/>
                <DataGridTextColumn Header="HEX" Binding="{Binding ColorHex}" Width="140"
                                    ElementStyle="{StaticResource CellText}"/>
                <DataGridTemplateColumn Header="" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Удалить" Style="{StaticResource GhostButton}"
                                    Padding="12,6" FontSize="12" Height="32"
                                    VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Click="Delete_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
