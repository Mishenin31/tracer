<UserControl x:Class="HabitTracker.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="36,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock x:Name="GreetingText" Style="{StaticResource H1}"/>
            <TextBlock x:Name="DateText" Style="{StaticResource Muted}" FontSize="15" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Сводка -->
        <Grid Grid.Row="1" Margin="0,24,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="14" Padding="20">
                <StackPanel>
                    <TextBlock Text="Привычки сегодня" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="HabitsProgressText" Foreground="{StaticResource AccentBrush}"
                               FontSize="30" FontWeight="Bold" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="14" Padding="20">
                <StackPanel>
                    <TextBlock Text="Активных задач" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="TasksOpenText" Foreground="{StaticResource PrimaryLightBrush}"
                               FontSize="30" FontWeight="Bold" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="4" Background="{StaticResource SurfaceBrush}" CornerRadius="14" Padding="20">
                <StackPanel>
                    <TextBlock Text="Лучшая серия" Style="{StaticResource Muted}"/>
                    <TextBlock x:Name="BestStreakText" Foreground="{StaticResource TextBrush}"
                               FontSize="30" FontWeight="Bold" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="2" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Привычки на сегодня" Style="{StaticResource H2}" Margin="0,0,0,12"/>
                <ItemsControl x:Name="HabitsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12"
                                    Padding="16,12" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="10" Height="40" CornerRadius="5"
                                            Background="{Binding ColorHex}"/>

                                    <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextBrush}"
                                                   FontSize="15" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding CategoryName}" Style="{StaticResource Muted}"
                                                   FontSize="12" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <Border Grid.Column="2" Background="{StaticResource SurfaceAltBrush}"
                                            CornerRadius="8" Padding="10,5" VerticalAlignment="Center" Margin="0,0,14,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🔥 " FontSize="13"/>
                                            <TextBlock Text="{Binding StreakDisplay}" Foreground="{StaticResource AccentBrush}"
                                                       FontWeight="SemiBold" FontSize="13"/>
                                        </StackPanel>
                                    </Border>

                                    <CheckBox Grid.Column="3" IsChecked="{Binding DoneToday}"
                                              VerticalAlignment="Center" Click="HabitCheck_Click"
                                              Tag="{Binding}" Width="26" Height="26"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock x:Name="EmptyHabits" Text="Привычек пока нет. Добавьте их во вкладке «Привычки»."
                           Style="{StaticResource Muted}" Visibility="Collapsed" Margin="0,8,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
